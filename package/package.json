{
  "name": "stream-gating",
  "version": "0.0.15",
  "description": "Utils, for stream gating with existential NFT-s",
  "author": "Superfluid Finance",
  "license": "MIT",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rimraf dist && cp -R ../typechain-types ./src && tsc -p ./tsconfig.build.json"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "ethers": "^6.7.0",
    "viem": "^1.5.4"
  }
}
